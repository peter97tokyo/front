
function FunctionComponent() {
  return (
    <div>
      <p>1. 함수형 컴포넌트(Function Component)</p>
      <ol>
        <li>
          자바스크립트 함수로 정의됩니다.
        </li>
        <li>
          useState, useEffect 같은 리액트 훅(Hook)을 사용해 상태 관리와 생명주기 기능을 구현할 수 있습니다.
        </li>
      </ol>
    </div>
  );
}

export default FunctionComponent;
